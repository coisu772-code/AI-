[project]
name = "ai-video-channel-production-contract-tools"
version = "0.4.0-dev.1"
description = "AI video channel production contracts and local validation tools"
requires-python = ">=3.10"
dependencies = [
  "jsonschema>=4.23,<5",
  "pyyaml>=6.0,<7",
  "pypdf>=6.0,<7",
  "python-docx>=1.2,<2"
]

[tool.uv]
package = false
