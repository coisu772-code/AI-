[project]
name = "ai-video-channel-production-contract-tools"
version = "0.11.0-rc.4"
description = "AI video channel production contracts, safe production handoff, publishing, and isolated analytics tools"
requires-python = ">=3.10"
dependencies = [
  "jsonschema>=4.23,<5",
  "pyyaml>=6.0,<7",
  "pypdf>=6.0,<7",
  "python-docx>=1.2,<2",
  "tzdata>=2026.1,<2027"
]

[tool.uv]
package = false
