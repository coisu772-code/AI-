# 0.1.0-beta.2 与阶段2未发布工作树兼容矩阵

本矩阵描述阶段 1 本地候选版，不表示三个既有桌面组件已经被打包进 GitHub Release。

| 组件 | 版本 | 是否随 0.1.0-beta.2 提供 | 状态 | 兼容说明 |
| --- | --- | --- | --- | --- |
| Codex 插件 | `0.1.0-beta.2` | 是 | Beta | 提供总入口与显式频道建库入口；不执行阶段 2 业务 |
| Windows 安装器 | `0.1.0-beta.2` | 是 | Beta | 支持安装、幂等重装、升级备份、回滚和卸载 |
| 跨中心契约 | `1.0.0` | 是 | 可验证 | 10 类契约，JSON Schema 2020-12，哈希规则 `canonical-json-v1` |
| 本地工具服务 | `0.0.0` | 否 | 骨架占位 | 阶段 2 才实现和接入 |
| 控制中心 | `0.11.0` | 否 | 外部基线 | 本阶段只登记，不修改、不打包 |
| 新漫剧工坊 | `1.0.0` | 否 | 外部基线 | 本阶段只登记，不修改、不调用正式生产 |
| YouTube 发布中心 | `0.1.0` | 否 | 外部基线 | 本阶段只登记，不修改、不上传 |

## 阶段2未发布工作树

下列能力只存在于 2026-08-04 的本地阶段2工作树，不改变已发布的 `v0.1.0-beta.2` 标签和 Release：

| 组件 | 版本／协议 | 状态 | 兼容说明 |
| --- | --- | --- | --- |
| Codex 插件候选 | `0.2.0-dev.1` | 隔离安装通过 | 两项阶段2 Skills、MCP 配置和本地工具入口；未发布 |
| Windows 安装器候选 | `0.2.0-dev.1` | 隔离安装通过 | 使用 draft 清单完成安装与幂等重装；正式运行时仍待集成 |
| 本地工具服务 | `0.2.0-dev.1` | 隔离验收通过 | stdio MCP；本地工具协议 `1.0.0`；需要兼容 Python 运行时 |
| 发布中心只读边界 | `youtube-publisher-center/channel-list/v1` | 隔离联合验收通过 | 正式 CLI、九字段白名单、结构化错误；程序安装发现待集成 |
| system.db | Schema `1` | 隔离验收通过 | 频道注册、真实身份唯一约束、任务单频道绑定 |
| channel.db | Schema `1` | 隔离验收通过 | Profile 版本、预设版本、本次覆盖记录、迁移历史 |
| `.avchannel` | `1.0.0` | 隔离验收通过 | 文件哈希、身份冲突、导入再验证、恢复失败回滚 |
| 预扫描音色目录 | `1.0.0` | 等待正式目录 | 只读消费，不启动工坊扫描；缺失时阻塞阶段 B |

阶段2 draft 事实源为 `release-manifests/release-v0.2.0-dev.1.json`。完整证据见 `docs/phase2-channel-library-validation-2026-08-04.md`。在正式运行时、真实音色目录和最终安装布局验收完成前，不得把该工作树称为新的可发布 Beta。

产品 `0.1.0-beta.2` 只接受 Schema 主版本 `1`、契约交换协议 `1.0.0` 和安装状态格式 `1.0.0`。统一机器可读事实源是 `release-manifests/release-v0.1.0-beta.2.json`。

## 发布通道

- `stable`：供明确完成验收的正式版本使用。
- `beta`：供不承诺向后兼容的试用版本使用。

当前 Beta 清单为 `published`，并绑定已通过干净克隆复验的源码提交 `acb8586775804bec25d05cdca74c2d141dea9878`。GitHub 预发布由版本标签触发的验证工作流生成。

## 阶段3未发布候选兼容矩阵

以下能力只属于 2026-08-04 本地 `0.3.0-dev.1` 工作树，不代表新的 GitHub Release。

| 边界 | 阶段3候选 | 兼容结论 |
| --- | --- | --- |
| Source Package | `1.0.0` | 主版本和 Schema 不变；采集细节保存在版本化资产与报告中 |
| 本地工具协议 | `1.0.0` | 阶段2工具保持兼容，仅新增 `source_*` 工具 |
| `system.db` | Schema `1` | 不变 |
| `channel.db` | Schema `2` | 从 Schema 1 备份后迁移；旧档案和生产预设表不变；见 CR-002 |
| 资料去重 | 平台 ID／规范 URL／SHA-256 | 同身份或同内容复用 Source Package，不重复保存正文资产 |
| 资料版本 | Source Library `1.0.0` | 来源内容或公开清单发生真实变化时递增修订版本 |
| YouTube | 公开只读、`yt-dlp` 可替换后端 | 人工字幕→自动字幕→允许且已配置的临时本地转录；无文字时阻断 |
| 本地文档 | TXT／MD／EPUB／PDF／DOCX | 保留原文件 SHA-256、编码、章节／页／段完整性和来源 |
| 小说网站 | 日／中／英各 3 站，manifest `1.0.0` | 商业站仅元数据和用户授权文件；公开全文必须逐作品权利证据和允许获取方式 |
| 隔离安装 | Windows 候选安装器 | 插件、MCP、三个 Skills 和日中英资料闭环可在隔离数据目录运行 |
| 内容生产 | 不提供 | 拆视频、拆书、仿写仅为后续可插拔能力，本阶段不执行 |
| 控制中心／工坊／发布中心 | 外部基线 | 不修改控制中心和工坊；发布中心只读频道接口保持阶段2边界 |

`0.3.0-dev.1` 仍是 draft。只有阶段3报告列出的全部回归、正式发布中心只读接口、隔离安装和退出条件均通过后，才可建议进入阶段4；这不构成发布、OAuth 或上传授权。

## 阶段4未发布候选兼容矩阵

以下能力只属于 2026-08-04 本地 `0.4.0-dev.1` 工作树，不代表新的 GitHub Release。

| 边界 | 阶段4候选 | 兼容结论 |
| --- | --- | --- |
| Topic Package | `1.0.0` | 在既有主版本内补强必填生产语义；锁定受众、证据、事实、大纲、逐集剧情、精确生产建议、七项评分、排名、选择、检查点和 G3 |
| Manuscript Package | `1.0.0` | 目标语言原生稿为唯一生产母稿；非中文逐行回译，中文不复制；锁定 Story Bible、角色、音色、合并质量门、映射和 G4 |
| Publishing Asset Package | `1.0.0` | 仅接受已确认母稿；锁定唯一标题、简介、8～12 Hashtags、5 个封面候选、唯一封面、CTR 联评和 G5 |
| Source Package 输入 | `CONTENT_READY`／明确接受的 `PARTIAL` | PARTIAL 必须逐来源记录接受时间和已知限制；保留 `fact`／`inference`／`unknown` 与来源引用 |
| 选题路由 | 原创／频道画像／大纲直通 | 当前可执行；频道路线严格 10 个真实检查点，普通原创 3～6 个，大纲直通 1 个 |
| 扩展路由 | 趋势／单作品／多作品／拆书／仿写 | 稳定接口存在；对应能力缺失时明确 `CONTENT_EXTENSION_UNAVAILABLE`，不得伪造 |
| 本地工具协议 | `1.0.0` | 保持阶段2/3工具兼容，新增 9 个 `content_*` 工具 |
| Codex Skills | 6 个 | 总路由、频道建库、资料库、选题、母稿、发布资产可安装并通过静态与隔离健康检查 |
| 合成验收包 | `ja-JP`／`zh-CN`／`en-US` | 同一 Schema、状态机、质量门、文件、哈希与映射验证；明确不是线上数据 |
| 长期学习 | 只读快照 | 一次性修改仅写项目；任何长期写回请求均被拒绝 |
| 制作与发布 | 不执行 | 只返回移交资格；不启动工坊、不授权、不上传、不读取 Analytics |

阶段4 draft 事实源为 `release-manifests/release-v0.4.0-dev.1.json`，验证报告为 `docs/phase4-content-loop-validation-2026-08-04.md`。该结论只覆盖阶段4内容最小闭环，不能解释为阶段5制作、真实发布或 GitHub Release 的许可。
