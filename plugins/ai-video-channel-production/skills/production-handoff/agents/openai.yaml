interface:
  display_name: "制作中心"
  short_description: "继承口播稿标题，规划剧情画面并移交工坊验收成片"
  default_prompt: "Use $production-handoff to keep the confirmed narration title as the publishing title, build the locked story visual plan, emotion framing, continuity, and budgeted prompts, then hand off to the workshop and stop at VIDEO_READY."

policy:
  allow_implicit_invocation: false
