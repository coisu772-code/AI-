interface:
  display_name: "拆书与迁移方向"
  short_description: "完整拆解叙事素材并生成不少于六个原创迁移方向和仿写交接卡"
  default_prompt: "Use `$content-deconstruct` to fully deconstruct the supplied narrative and prepare the rewrite handoff."

policy:
  allow_implicit_invocation: false
