interface:
  display_name: "发布素材中心"
  short_description: "Create confirmed titles metadata thumbnails and CTR review"
  default_prompt: "使用 $publishing-assets 从已确认母稿生成并联合确认发布素材包。"

policy:
  allow_implicit_invocation: false
